// Package registry is the Tool Registry: registered capabilities (e.g. github.search, filesystem.read), their permissions, health, and latency, keyed independently of any single provider.
//
// Scaffolded in Milestone 1 as an empty package to establish the
// repository's domain boundaries; behavior lands in Milestone 5 (Gateway and protocol adapters).
package registry
