// Package timeline projects an Execution's immutable event stream into the human-readable Timeline shown in the dashboard (e.g. '14:02 Execution Started', '14:06 Budget +3500 Tokens'). The timeline is a read-model built from events, never a source of truth itself.
//
// Scaffolded in Milestone 1 as an empty package to establish the
// repository's domain boundaries; behavior lands in Milestone 3 (Event bus and timeline engine).
package timeline
