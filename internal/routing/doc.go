// Package routing is the Model Router: provider-abstraction routing across models with fallback, failover, weighted routing, cost-aware routing, capability matching, and automatic downgrade. Explicitly out of scope for Phase 1 per the spec; scaffolded now so the package boundary is reserved.
//
// Scaffolded in Milestone 1 as an empty package to establish the
// repository's domain boundaries; behavior lands in Post-Phase-1 (Model Router).
package routing
