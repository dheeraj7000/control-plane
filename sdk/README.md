# sdk

Reserved for client SDKs (Go, TypeScript) generated from or hand-written
against `api/openapi.yaml`. Not started — SDK generation is only useful
once the API surface stabilizes past Milestone 5.
