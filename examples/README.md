# examples

Reserved for example workflow definitions and end-to-end usage samples
(e.g. "register an agent, run a workflow through the MCP adapter, watch
its timeline"). Populated starting Milestone 2 once the workflow
definition format is decided (see `docs/architecture.md#open-questions`).
